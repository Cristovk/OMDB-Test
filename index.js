
import App from './App'; // Import your main App component

// Expo doesn't require manual registration with bundlers like Metro
// registerRootComponent(App); // This line might be unnecessary in Expo

if (__DEV__) {
  // Code for development-specific setup, if needed (optional)
}

export default App;
